import Layout from "../components/Layout";

import Graph from "../components/Graph";

const AboutPage = () => {
  return (
    <Layout>
      <Graph />
    </Layout>
  );
};

export default AboutPage;
